import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.thinkeveryday.app',
  appName: 'Think Everyday For You',
  webDir: 'out'
};

export default config;
